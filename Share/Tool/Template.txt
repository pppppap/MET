// @formatter:off
(ConfigDesc)

using System.Collections.Generic;
using System.Collections.ObjectModel;
using MongoDB.Bson.Serialization.Attributes;
using ProtoBuf;

namespace ET
{
    [ProtoContract]
    public partial class (ConfigName): ProtoObject, IConfig
    {
(Fields)
    }

    [ProtoContract]
    [Config]
    public partial class (ConfigName)Category: ConfigSingleton<(ConfigName)Category>
    {
        [BsonElement]
        [ProtoMember(1)]
        public IList<(ConfigName)> List { get; private set; } = new List<(ConfigName)>();

(DictOp)

        [ProtoAfterDeserialization]
        public void ProtoEndInit()
        {
            this.List = new ReadOnlyCollection<(ConfigName)>(this.List);

(DictInit)

            this.AfterCategoryInit();
        }
    }
}